import { ReactElement, FC } from "react";

const Home: FC = (): ReactElement => {
  return <img src="/images/hero-image.jpeg" alt="Web Design" className="object-fit-cover vw-100 vh-100" />;
  // return <Carousel />;
};
export default Home;
